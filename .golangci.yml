version: 2

run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - errcheck
    - staticcheck
    - unused
    - ineffassign
  disable:
    - govet

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
